<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.project.mapper.ReplyMapper">	
    	<!-- 댓글 등록 -->
    	<insert id="insert">
    		insert into trade_reply(bno, reply, replyer)
    		values (#{bno}, #{reply}, #{replyer})
    	</insert>
    	
    	<!-- 댓글 조회 -->
    	<select id="read" resultType="com.project.model.ReplyVO">
    		select * from trade_reply where rno = #{rno}    		
    	</select>
    	
    	<!-- 댓글 삭제 -->
    	<delete id="delete">
    		delete from trade_reply where rno = #{rno}
    	</delete>
    	
    	<!-- 댓글 수정 -->
    	<update id="update">
    		update trade_reply set reply = #{reply}, updateDate = now() where rno = #{rno}
    	</update>
    	
    	<!-- 특정 게시물의 댓글 조회 -->
    	<select id="getListPaging" resultType="com.project.model.ReplyVO">
    		select * from trade_reply where bno = #{bno} order by rno asc
    	</select>
</mapper>